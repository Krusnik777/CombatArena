namespace CombatArena.Game.Root
{
    public static class GameplayExitTags
    {
        public const string RESTART = nameof(RESTART);
        public const string NEXT = nameof(NEXT);
        public const string EXIT = nameof(EXIT);
    }
}
